{
  "$schema": "https://opencode.ai/config.json",
  "$comment": "OpenCode V2 — merge this mcp block into ~/.config/opencode/opencode.json or the project opencode.json. V2 nests servers under mcp.servers and uses disabled instead of enabled.",
  "mcp": {
    "servers": {
      "beatdesign": {
        "type": "local",
        "command": [
          "/absolute/path/to/Beat Design/node_modules/.bin/tsx",
          "/absolute/path/to/Beat Design/scripts/mcp-server.ts"
        ],
        "cwd": "/absolute/path/to/Beat Design",
        "disabled": false
      }
    }
  }
}
